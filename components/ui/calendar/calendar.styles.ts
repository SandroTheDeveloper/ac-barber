import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 12,
  },
  selectedInfo: {
    marginTop: 8,
    textAlign: "center",
    fontWeight: "600",
  },
});
